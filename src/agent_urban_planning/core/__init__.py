"""Core simulation primitives: Environment, AgentPopulation, Market, SimulationEngine.

Phase 2 of the package extraction (see ``MAPPING.md``) populates this subpackage
by porting modules from the dev repo's ``simulator/`` package. Until then the
package is intentionally empty.
"""
