"""Bundled builtin scenarios.

Two scenarios ship with the package:
  * ``singapore_real_v2`` — small Singapore real-data example.
  * ``berlin_2006_ortsteile`` — Berlin 96-zone scenario for the paper's main
    results.

Loaded via :func:`load`::

    from agent_urban_planning.data import builtin
    scenario = builtin.load("singapore_real_v2")
"""
